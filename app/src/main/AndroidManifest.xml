<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.insacvl.educations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AdminActivity"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".SoloActivityHard"></activity>
        <activity android:name=".SoloActivityMedium" />
        <activity android:name=".SoloActivityDifficultySelector" />
        <activity android:name=".SelectKidSoloActivity" />
        <activity android:name=".SelectKidAdminActivity" />
        <activity android:name=".AddWordActivity" />
        <activity android:name=".SoloActivityEasy" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>